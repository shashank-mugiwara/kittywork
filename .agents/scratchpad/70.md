# Issue #70 — Planning Notes

## Summary
Add health check endpoint verification test: verify `GET /health` returns `200 OK`.

## Sub-Issues & Dependency Chain
1. **M1 — Setup**: Scaffold test infrastructure (no deps)
2. **M2a — Core Impl**: Implement `/health` endpoint (depends on M1)
3. **M2b — Integration**: Wire endpoint into test context (depends on M2a)
4. **M3 — Tests**: Write automated `200 OK` assertion test (depends on M2b)
5. **M4 — Docs**: Update AGENTS.md, ARCHITECTURE.md, README (depends on M3)

## Key Decisions & Assumptions
- Tech stack is GENERIC — agent must inspect repo root for build files (pom.xml, requirements.txt, package.json) before choosing libraries
- Use only libraries already declared in existing build files
- No hardcoded URLs — use env vars or test config
- If `/health` endpoint already exists, M2a documents and validates it rather than re-creating it
- Health response contract: HTTP 200, JSON body `{"status": "ok"}` (or match existing schema)

## Project Rules Applied
- Never delete/overwrite existing files without user confirmation
- Conventional Commits format required
- AGENTS.md and docs/ARCHITECTURE.md must be updated (M4)
- No new global state or singletons

## Milestones Created
- M1: Setup & Scaffolding (due 2025-07-25)
- M2: Core Implementation (due 2025-08-01)
- M3: Testing & Review (due 2025-08-08)
- M4: Documentation & Deployment (due 2025-08-15)
